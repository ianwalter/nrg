name: Changeset
on:
  pull_request:
    types: [labeled, synchronize]
jobs:
  changeset:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: Add Changeset
        uses: generates/changeset-action@1a30464ba81f41a759f4b0cff012bfb274938e64
        with:
          token: ${{ secrets.GH_PAT }}
      - name: Commit Changes
        uses: generates/commit-action@v1.0.2
        with:
          token: ${{ secrets.GH_PAT }}
